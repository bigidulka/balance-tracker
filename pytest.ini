[pytest]
pythonpath = .

# Only the curated regression suite is collected. Reverse-engineering probes and manual
# diagnostics live under scripts/ and are executed directly (see scripts/research READMEs).
testpaths = tests
